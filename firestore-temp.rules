rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Test collection - 認証なしでもアクセス可能
    match /_test/{docId} {
      allow read, write: if true;
    }
    
    // 一時的に全ての認証済みユーザーにアクセス許可（テスト用）
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}