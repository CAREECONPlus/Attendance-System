rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Test collection - 認証なしでもアクセス可能
    match /_test/{docId} {
      allow read, write: if true;
    }
    
    // MVP用：認証済みユーザーは全データアクセス可能
    // （本格運用前にセキュリティ強化）
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}