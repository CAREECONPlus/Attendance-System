rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Test collection - 認証なしでもアクセス可能（テスト用）
    match /_test/{docId} {
      allow read, write: if true;
    }
    
    // 認証済みユーザーは全てアクセス可能（一時的）
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}